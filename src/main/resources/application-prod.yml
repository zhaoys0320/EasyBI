server:
  port: 8088
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.152.5.222:3306/easybi
    username: root
    password: Shuo2001
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: localhost
    port: 6379
    timeout: 5000
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''